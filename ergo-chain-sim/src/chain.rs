use crate::Block;

/// Ergo blockchain(UTXO) simulation
pub struct ChainSim {}

impl ChainSim {
    /// Add a new block to the chain (head/latest)
    pub fn add_bock(&self, _block: Block) {
        // TODO
    }
}
